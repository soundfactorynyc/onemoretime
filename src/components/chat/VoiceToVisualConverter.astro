---
// Voice to Visual Converter - Chat Enhancement Component
---

<div class="voice-visual-converter">
    <button class="component-btn voice-btn" id="voiceVisualBtn">
        <div class="component-icon">🎤</div>
        <div class="component-label">Voice Visual</div>
        <div class="premium-tag">PREMIUM</div>
    </button>
</div>

<style>
.voice-visual-converter {
    width: 100%;
}

.component-btn {
    width: 100%;
    background: #1a1a1a;
    border: 1px solid #333;
    border-radius: 8px;
    padding: 12px;
    color: #ffffff;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
}

.component-btn:hover {
    background: #2a2a2a;
    border-color: #555;
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
}

.component-icon {
    font-size: 20px;
    margin-bottom: 4px;
}

.component-label {
    font-size: 11px;
    font-weight: 500;
    text-align: center;
}

.premium-tag {
    position: absolute;
    top: -4px;
    right: -4px;
    background: #ff4757;
    color: white;
    font-size: 8px;
    padding: 2px 6px;
    border-radius: 10px;
    font-weight: bold;
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.1); opacity: 0.8; }
    100% { transform: scale(1); opacity: 1; }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', () => {
    const voiceBtn = document.getElementById('voiceVisualBtn');
    
    voiceBtn?.addEventListener('click', () => {
        alert('🎤 Voice to Visual Converter\n\nUpgrade to PREMIUM to unlock:\n• Real-time voice visualization\n• Custom waveform styles\n• Audio-reactive particles');
    });
});
</script>
